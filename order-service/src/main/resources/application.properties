spring.application.name=order-service
server.port=8083

# ===============================
# PostgreSQL (this service's own database - see ../docker-compose.yml postgres-order,
# mapped to host port 5434 since 5432/5433 are already claimed)
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5434/order_service_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ===============================
# Redis
# ===============================
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.cache.type=redis

# ===============================
# Kafka (shared broker across all services - produces order-events only)
# ===============================
spring.kafka.bootstrap-servers=localhost:9092

spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# ===============================
# user-service (synchronous internal call at order-creation time)
# ===============================
user-service.base-url=http://localhost:8081

# ===============================
# Logging
# ===============================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# ===============================
# JWT (must be byte-for-byte identical across every service that validates tokens -
# see docs/MICROSERVICES.md "Auth handling")
# ===============================
jwt.secret=your-super-secret-key-change-this-in-production
jwt.expiration=3600000
